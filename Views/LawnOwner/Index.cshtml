@model IEnumerable<Clipz.Models.LawnOwnerModels.LawnOwnerListItem>

@{
    ViewBag.Title = "Index";
}
<br />  
<h2>Registered Lawn Owners</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LawnOwnerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YardWorkId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LocationId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LawnOwnerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YardWorkId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location.State)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.LawnOwnerId  }) |
            @Html.ActionLink("Details", "Details", new { id = item.LawnOwnerId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.LawnOwnerId })
        </td>
    </tr>
}

</table>
